interface:
  display_name: "Prototype"
  short_description: "a design question about state, logic, or UI needs a small throwaway program to produce a runnabl"
  default_prompt: "Use $prototype for this task."
policy:
  allow_implicit_invocation: true
