interface:
  display_name: "Resolving Merge Conflicts"
  short_description: "you need to resolve an in-progress git merge/rebase conflict."
  default_prompt: "Use $resolving-merge-conflicts for this task."
policy:
  allow_implicit_invocation: false
